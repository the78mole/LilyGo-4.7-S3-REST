[project]
name = "epd-client-tools"
version = "0.1.0"
description = "REST client for the LilyGo T5-4.7 S3 e-paper firmware"
requires-python = ">=3.9"
dependencies = [
    "requests>=2.31.0",
    "Pillow>=10.0.0",
]

[tool.uv]
package = false
